# 更新日志

本项目的所有显著变更记录在此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/):`主版本.次版本.修订号`。

1.0 之前属于早期阶段,次版本号的提升可能包含破坏性变更。

---

## [0.4.0] — 2026-07-29

**任务记录按对话折叠 —— 一份材料的「一次工作」是整个对话,不是某一轮**

一份材料很少一次成型,总要多轮打磨。这一版把「一次工作」的单位从**单轮调用**
改成**整个对话**,并补上了几处让产出真正可用的细节。

### 新增

**任务记录按对话折叠**

- 一个对话折叠成一行,显示总轮数、合计花费、全部产出;展开后每轮仍用自己那轮的原话当标题
- 折叠行的状态取**最后一轮**;对话与一次性任务(知识编译/文档提取/定时任务)
  按最后活动时间**统一排序**,不分段
- 筛选标签的计数随之改为**折叠后的行数** ——「对话 6」指 6 个对话而非 6 轮,
  否则标签数字和列表行数对不上
- 分组在后端完成:前端分组时 `limit` 会把一个对话拦腰截断

**交付物识别**

- 此前只认 `customers/…` 这类相对路径开头的产出,而模型经常回绝对路径,一个都抓不到。
  改为宽松匹配后**靠文件是否真实存在过滤**,绝对路径自动转成相对工作台
- 框架文件(`CLAUDE.md`、`bin/` 等)不计入交付物

**产出落盘约定**(CLAUDE.md)

- 产出必须落数据区,**绝不写工作台根目录**;归属不明确的一律落 `inbox/`
- 回答里要给相对路径,界面靠它生成可点链接

**「从零重做」勾选**(对话框下方)

- 工作台内的历史产出 AI 都读得到,不说它就会拿来改。勾上则要求从零重做,
  不参考 `inbox/`、`projects/`、`archive/` 下的已有版本
- 默认不勾且不记忆 —— 免得某次勾了忘关,后续需要迭代的任务全被当成重做

**AI 追问的选项可点击**

- 对话里的 markdown 列表项点一下即追加到输入框,尾部括号里的说明自动去掉
- 不要求模型按固定格式应答,换任何模型都有效

**任务记录显示模型** —— 供应商与模型名放在元信息最前。切过供应商后,
「这条是谁跑的」比耗时更需要一眼看到

**Bash 跑完自动关闭** —— 临时开启后,任务结束即自动关回。判断在后端做
(用户可能关掉页面就走),且要等所有任务都停 —— 否则先完成的任务会把还在跑的那个的工具抽走

### 修复

- 收件箱空时侧栏计数不刷新;`Read(~/*)` 的通配符曾把工作台自身封死(v0.3.2 已修,此处补记)
- 技能导入的标识不再要求手敲:选完文件自动填并规范化大小写

### 更正:关于禁用 Bash 的说法

此前 README 与发布说明称「Bash 默认禁用**以防绕过路径规则**」,**这个说法不准确**。
实测(开启 Bash 后执行 `ls ~/Desktop`、`head .mask/map.json`)全部被拒,
提示为 `Permission to use Bash with command … has been denied` ——
**路径规则本身就覆盖了 Bash 命令里的路径**。

禁用 Bash 真正挡住的是路径规则管不到的事:

- 用 python 库直读 **docx/xlsx 原件**里的真名 —— 那些原件就在允许读的项目目录里,
  内容改不掉,Read 工具因为是二进制读不了,Bash 能读
- 联网外发

已一并更正 README、界面提示与相关文案。

---

## [0.3.3] — 2026-07-29

**AI 追问时给的选项,点一下就填进输入框。**

### 新增

- AI 回复里的列表项在对话中**可点击**,点一下把该项文字追加到输入框,
  点几个再发送即可。AI 追问「要 HTML 还是 PPT / 给谁看 / 用什么 logo」这类
  多选题时,不用再手打一遍
- 尾部括号里的说明自动去掉(「HTML 单页(适合网页浏览)」只填「HTML 单页」)——
  那些括号是给人看的,不必发给模型
- **不要求 AI 输出特定格式**:只要用了 markdown 列表就能点,换任何模型都有效。
  做成结构化表单需要模型按固定格式应答,而工作台支持切换第三方模型,约束不住
- 仅在 AI 对话里生效,md 编辑器预览与纪要正文的列表不受影响

---

## [0.3.2] — 2026-07-28

**AI 能读到磁盘上的哪些地方,默认收到只剩工作台一个目录。**

### 新增

**磁盘范围(AI 任务页)**

- 此前 `permissions.deny` 全写成相对路径(`./knowledge/...`),只管得住工作台内部。
  而 `bypassPermissions` 下 Claude Code 不再拦工作目录之外的读取 ——
  **实测一个 AI 任务成功列出了家目录下 580+ 个文件**
- 改为**默认全拒、例外逐项放行**:家目录下除工作台自身外的所有顶层目录一律拒绝,
  以后新增的目录自动挡住,不需要回头补名单
- 新增界面入口列出家目录的真实文件夹供勾选,并支持追加任意绝对路径
  (外挂硬盘、共享盘等不在家目录下的位置)
- 知识库仍按原有的目录白名单单独放行,不受这层影响

**技能导入体验**

- 选完文件自动填技能标识,大写 / 空格 / 下划线自动规范化
  (`Product-Promo-Kit` → `product-promo-kit`)
- 字段顺序改为先选文件再显示标识 —— 此前要求先填标识,而那时用户还没选文件
- 标识不合法时说明转换结果与具体原因,不再只回一句「技能标识不合法」

### 修复

- 家目录散落文件的拒绝规则写成了 `Read(~/*)`,而 glob 的 `*` 连目录一起匹配,
  **把工作台自身也封死了**(实测 `customers/` 与已放行的知识库目录全部被拒)。
  改为逐个列出非目录项
- 家目录路径此前只写绝对路径形式,实测匹配不上,AI 用 Glob 照样能列出目录。
  改为 `~/` 与绝对路径两种写法并存

### 说明:这一版的边界是实测出来的

四次实测,前三次都发现了问题:

1. 相对路径规则管不到工作目录之外 → AI 列出了 580+ 个文件
2. 改用绝对路径后仍不匹配 → Glob 照样列出目录
3. 补上 `~/` 写法后拦住了,但通配符误伤 → 工作台自己也读不了
4. 逐个列非目录项 → 五项测试全部符合预期

最终验证结果(拒绝信息为 CLI 原文):

| 目标 | 期望 | 实测 |
|---|---|---|
| `~/Desktop` | 拒 | `Permission to read /Users/…/Desktop has been denied.` |
| `~/claude`(代码目录) | 拒 | 同上 |
| 工作台 `customers/` | 通 | 通 |
| 知识库已放行目录 | 通 | 通 |
| 知识库未放行目录 | 拒 | 拒 |

---

## [0.3.1] — 2026-07-28

**收件箱独立成页,侧栏页脚不再占掉三分之一。**

### 变更

- **收件箱从知识库里拆出来**,成为独立页面 `#/inbox`。它是「还没决定放哪」的中转区,
  而知识库是「已经归好类」的资料 —— 塞在知识库的目录树里会让人以为它是其中一个文件夹
- 收件箱**空的时候也显示**。此前空则整条不渲染,形成死结:想用它得先看到它,
  想看到它得先往里放文件,而放文件的入口就在它身上
- **侧栏页脚从 7 行压到 3 行**:claude 与 MCP 状态合并一行,模板包与工作台版本合并一行,
  收件箱 / 回收站 / 映射表三个次级入口并排一行。侧栏是导航区,不该被状态信息占掉三分之一

### 修复

- 侧栏页脚的次级入口横向溢出到侧栏之外 —— 导航项样式 `#side a` 带 `display:block`
  与 `padding:8px 22px`,新样式没显式覆盖,三个链接各多出 44px 内边距
- 知识库树里点已被删除的文件夹时提示「打开失败」,实际是文件不存在;
  错误消息直接把路径当文案,读起来像权限问题

---

## [0.3.0] — 2026-07-28

**客户信息在进入 AI 之前就换成假名,而 AI 能读到哪些目录由你逐项指定。**

这一版的主题是:让工作台可以和公网大模型一起用,同时把「哪些数据出去、以什么形态出去」
变成你可管可控、且可验证的东西。

### 新增

**客户侧脱敏 —— 录入即生效**

- 表单里照常填真名,**落盘那一刻就换成可读假名**:`示例银行 → sl银行`、`张三丰 → 张sf`。
  保留"是什么"(银行/证券/姓),抹掉"是谁";同音冲突自动编号(`sl银行2`)
- 真名只存在 `.mask/map.json` **一处**,600 权限、每次写入前自动备份、不进版本库。
  磁盘上的数据文件本身就是假名 —— 不做"明文存盘 + 出口脱敏",少一个明文副本就少一条泄露路径
- 覆盖 11 个写入端点:客户、项目、纪要(客户方参会人逐个登记,**我方参会人保留真名**)、
  协作、日历、待办、上传文件名与正文、定时任务指令、以及你在 AI 任务框里打的每一句话
- 联系方式类字段(地址 / 网址 / 电话 / 微信邮箱)**直接不存**,后端强制丢弃,绕不过
- 搜索照常用真名:查询词先翻成假名再去磁盘搜

**映射表页面** —— 全站唯一显示真名的地方

- 增 / 改 / 删条目,补别名(简称不补就会明文落盘,未补的行标黄提示)
- 「复制对照表」导出替换清单,按长度倒序排好 —— 人工替换必须从长到短,否则短假名会啃掉长的前半截
- 类型区分客户机构 / 客户方人员 / 厂商竞品,**我方同事与产品不登记,因此天然不被替换**

**AI 可读范围 —— 工具层拦截,不是提示词约定**

- 知识库按子目录两列独立勾选:**AI 可读** 与 **提取时脱敏**,都由你决定,程序不推断
- 未放通的目录连同其提取文本被 `permissions.deny` 硬性拒绝,**Read / Grep / Glob 一并受约束**
- 映射表、凭证文件、回收站同样在拒绝清单里
- **Bash 默认禁用** —— 它是读 docx/xlsx 原件和跑脚本的唯一途径,需要时可在 AI 任务页临时开启
- 项目交付物的提取文本(`.extracted/`)连同文件名一起脱敏 —— AI 必须能读它们,所以封不得,只能换名

**导出提示** —— 导出后列出这份文件里具体有哪几个假名要人工换回,并给可复制的清单。
工作台**不自动还原**,留一道人工确认

**客户归档与引用完整性**

- 客户新增归档入口,可随时恢复;名下还有在营项目时,删除与归档都会被拒绝并列出是哪几个
- 引用了已不存在客户的项目在列表里标「客户已不存在」

**收件箱** —— 从知识库树里提到侧栏常驻入口,空的时候也显示(此前空则整条不渲染,
想用它得先看到它、想看到它得先放文件,而放文件的入口就在它身上)

**知识库全量提取** —— 忽略增量缓存重新提取全部文档。重整目录结构、或改了脱敏设置之后需要跑

**任务状态细化**

- 新增 **中断**(`blocked`)与 **超时**(`timeout`)两个终态。此前模型被权限规则拦下、
  或撞上轮次/输出长度上限,任务照样记成「完成」—— 列表看着没事,点开才发现它在求授权
- 中断原因来自 CLI 返回的机器信号(`permission_denials` / `subtype` / `stop_reason`),
  不靠猜文本;行内直接显示停止原因与被拒工具,并给出下一步怎么办
- 中断 / 超时 / 失败的任务可一键「重试」;属于某条对话的任务重试时自动续上上下文
- 任务行与对话气泡显示耗时、输入/输出 token、本次花费、模型内部轮次
- 从对话框发起的任务归入独立的「对话」分类,标签可点击跳回对应对话

### 修复

- **映射表并发写会互相覆盖**:每个写操作都是「读全表 → 改 → 写回」,而服务是多线程的。
  已加线程锁 + 文件锁(跨进程,CLI 与服务可能同时跑),40 条并发写压测零丢失
- **反向还原按错的长度排序**:`@C1` 会先吃掉 `@C1-P1` 的前半截,还原出「示例银行-P1」这种残骸。
  两个方向都改为按"被查找串"长度倒序
- 映射表损坏时抛 `SystemExit`,在服务的请求线程里会直接把线程干掉、前端只看到连接断开。
  改为普通异常,正常报错
- 上传模板时因变量名错用报 `NameError`(脱敏逻辑插错了端点)
- 项目目录名重复客户名(`sl银行-sl银行-xxx`)
- 界面上「grep 类操作需要放通 Bash」的提示不准确 —— Grep/Glob 是内置工具,不经过 Bash,
  管住它们的是路径规则
- 开发版新增同目录模块(`bin/mask.py`)后同步到开源版会漏掉,导致服务起不来。
  同步脚本改为自动检测 `server.py` 的同目录 import,缺文件直接失败
- 脱敏脚本此前假设「每个真实机构名恰好出现一次」,占位符一改就误报、
  反而漏过真正的残留。改为**只校验结果**:替换多少次不管,脱敏后仍能搜到就失败

### 说明:脱敏能做到什么、做不到什么

拿这套机制对外说明时,以下三点不要略过:

- **可读假名不是不可识别。**`sl银行` 在银行业基本能被猜出来。这是为可用性做的取舍
- **只覆盖"谁",不覆盖"什么"。**客户名、人名换掉了,但技术方案、报价逻辑、SOW 正文
  原样进入 AI 上下文 —— 这部分脱敏在原理上解决不了
- **二进制原件里的真名去不掉。**只能保证 AI 读不到(禁 Bash + 只给它读脱敏后的提取文本)

---

## [0.2.0] — 2026-07-28

这一版把 AI 从「提交任务」变成「对话」,并让工作台不再绑死 Claude 订阅。

### 新增

**模型与供应商切换**(AI 任务页)

- 走 Anthropic 官方时可切 `opus` / `sonnet` / `fable` / `haiku`,留空则跟随 claude CLI 自身设置
- 内置 **DeepSeek / Kimi / 自定义网关** 预设:工作台**不自研 API 适配器**,而是给 claude CLI 注入
  Anthropic 兼容协议的环境变量(`ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_MODEL`)。
  CLI 原封不动,读写文件、Skills、多轮等 agent 能力完整保留
- 「测试连通性」按钮:实发一句最短的话(不碰工具),返回耗时与回复
- 没有 Claude 订阅的人填自己的 key 即可使用全部 AI 功能

**对话式 AI**

- 消息以气泡留存,**发完不再凭空消失**;提交失败会把内容还回输入框
- **追问真的带上下文**:用 `claude --output-format json` 取回 `session_id`,下一轮 `--resume` 续同一会话。
  「把第三章展开写」这类追问接得上,不是把气泡摞在一起
- 每轮显示供应商 / 模型 / 本轮花费,续话轮标「↩ 续上文」
- 「＋ 新对话」开新线程,顶部下拉切历史对话,当前对话存 localStorage,刷新不丢
- 回车发送、Shift+回车换行,输入框随内容长高

**任务归档与删除**

- 任务列表每行有「归档」与删除;归档只是从列表收起,**仍留在所属对话里**,可取消
- 筛选栏出现「已归档 N」入口与「清空已归档」
- 对话可整条删除(连同全部轮次)
- 运行中的任务一律拒绝归档 / 删除

**版本信息与发版自动化**

- 侧栏显示当前版本号,来源为 `workbench.json → version`
- 新增本更新日志
- `bin/release.sh <版本号>` 一条命令完成发版:改版本号 → 提交 → 打标签 → 推送
- 推送 `v*` 标签后 GitHub Actions 自动建 Release,发布说明取自本文件对应段落
  (用 runner 自带 `gh` 与内置令牌,不引第三方 action、无需配置密钥)

### 变更

- AI 命令模板默认加 `--output-format json`(用于取 `session_id` 与成本)。
  换成不支持该参数的 CLI 时去掉即可,工作台会自动退回纯文本模式
- README「已知限制」中「AI 成本不可控,无 token 统计」改为「按轮可见但无预算约束」——
  每轮花费已显示,但仍无额度上限
- `CLAUDE.md` 补充模型与供应商插槽的行为约定,明确凭证不得写入可分发文件

### 修复

- **删除的任务重启后复活**:`jobs.jsonl` 是追加式日志,只改内存不重写文件,
  下次启动会把删掉的记录读回来。改为用临时文件原子替换重写
- **任务运行途中被删导致记录回写**:`run_job` 收尾前先确认 id 是否仍存在
- **供应商「填 key + 同时启用」失败**:校验时重新读磁盘,看不到同一请求里刚填的 key。
  改为用未落盘的配置校验
- **输入框首屏渲染成 180px**:布局未稳时读 `scrollHeight` 会得到整块高度,
  空内容改为显式回位 46px
- 对话列表异步到达时,顶部切换器在首屏渲染中丢失

### 安全

- 第三方模型 API key 只写入 `.workbench/providers.json`(文件权限 `600`、已 gitignore),
  不进 `workbench.json`、不进 git、分享工作台时不会带走
- **任何 API 响应只回传脱敏值**(如 `sk-abc…1234`),明文不出后端
- 切回 Anthropic 官方时清空全部第三方环境变量,避免上一次切换的残留串到官方端点
- 换供应商后不跨账号续话(session 存在各家自己的历史里,硬 resume 必然失败),
  改为重开上下文并在气泡下注明原因

---

## [0.1.0] — 2026-07-27

首个开源版本。从生产工作台剥离出的干净副本,含全部框架代码与一套虚构 demo 数据。

### 新增

- **智能看板**:客户 / 商机 / 预算 / 累计签约统计,阶段分布(点击联动、可下钻),
  规则化风险提示,To Do 汇总
- **客户**:结构化档案(组织架构、决策链、联系方式、客户 KPI)+ 叙事档案
- **项目**:预算 / 合同额 / 签约日期,01~07 交付物流水线(上传 / 从模板创建 / AI 生成),归档
- **协作**:售前 × 销售 / 产研 / 交付三条线,每条动作强制绑定项目
- **纪要**:表单录入、文件导入、纪要瀑布流、行动项双向同步 To Do、周报
- **日历**:月视图 + 列表,一句话 AI 建日程,到点系统通知
- **场景库**:按行业沉淀业务场景,含 HTML 交互演示
- **技能**:技能库、第三方导入、技能市场,多技能可组合为「专家角色」
- **知识库**:把 PDF / Word / PPT / Excel 提取成可检索文本,编译成互链 wiki,全文搜索
- **AI 任务**:自由指令、按角色执行、定时任务、任务分类与检索
- **回收站**:删除进 `.trash/`,可恢复
- 一键部署脚本 `setup.sh`(`--check` 只体检,`PORT=` 换端口)
- 项目介绍页 `docs/index.html`,可发布为 GitHub Pages

[0.3.0]: https://github.com/mooncirclez/presales-workbench/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/mooncirclez/presales-workbench/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/mooncirclez/presales-workbench/releases/tag/v0.1.0
